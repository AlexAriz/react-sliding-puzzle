In order to run the project, follow the instructions:
* Change to the root directory of it.
* Run "npm install" to install all the necessary dependencies.
* Run "npm start" to start the server and open the application in a new browser.